#!/usr/bin/python
# -*- coding: utf-8 -*-

def handler(params):
    print("entry test module handler...")
    return


"""
def usage(p):
  print USAGE_EXAMPLE

def test(p):

  global USER_PWD
  USER_PWD = getpass.getpass("password for " + USER_NAME + ": ")

  
  url = "http://psci.emea.nsn-net.net:8080/view/LRC/job/PS_Release_Promo_FBLRC1606/changes"
  pronto_get_info("PR180075",USER_NAME,USER_PWD)
  
  response = urllib2.urlopen(url).read()

  f = open("test.txt", "w")
  f.write(response)
  f.close()


def encode_str(str):
    salt = commands.getoutput("whoami") 
    m = hashlib.md5()   
    m.update(str)
    return m.hexdigest()

def decode_str(str):
    m = hashlib.md5()   
    m.update(str)
    return m.hexdigest()
"""
